language: node_js
node_js: 
  - '12'

services:
  - docker

before-install: 
  - docker-compose -f docker-compose.yaml -p up --build --no-start
deploy:
  provider: script
  script: bash docker_bash
  on:
    branch: master
